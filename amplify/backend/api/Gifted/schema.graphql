type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  Email: AWSEmail
  Password: String!
  ListItems: [ListItem] @hasMany(indexName: "byUser", fields: ["id"])
}

type ListItem @model @auth(rules: [{allow: public}]) {
  id: ID!
  Link: AWSURL
  Price: String
  ShortDescription: String
  userID: ID! @index(name: "byUser")
} 